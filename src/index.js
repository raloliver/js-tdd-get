import { search, searchAlbums, searchArtists, searchPlaylists, searchTracks } from './search';
import { getAlbum, getAlbums, getTracks } from './album';

module.exports = {
  search, searchAlbums, searchArtists, searchPlaylists, searchTracks, getAlbum, getAlbums, getTracks
}
